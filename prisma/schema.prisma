generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Cosecha {
  id                   Int                   @id @default(autoincrement())
  fecha                DateTime
  kilosCosechados      Float
  cantidadCosechadores Int
  lotes                String
  totalHectareas       Float
  tipoCosecha          String
<<<<<<< HEAD
  createdAt            DateTime              @default(now())
  updatedAt            DateTime              @updatedAt
  kilos_diarios        Float?
  kilos_mensuales      Float?
  kilos_quincena       Float?
  tipo_cosecha         String?
  trabajadorId         Int?
  Trabajador           Trabajador?           @relation(fields: [trabajadorId], references: [id])
  cosechaLotes         CosechaLote[]
  procesos             ProcesoTrazabilidad[]
}

model Lote {
  id                 Int           @id @default(autoincrement())
  codigo             String        @unique
=======

  trabajadorId        Int?
  trabajador          Trabajador? @relation(fields: [trabajadorId], references: [id])
  tipo_cosecha        String?
  kilos_diarios       Float?
  kilos_quincena      Float?
  kilos_mensuales     Float?

  procesos ProcesoTrazabilidad[]

  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt
  cosechaLotes CosechaLote[]
}

model Lote {
  id                 Int      @id @default(autoincrement())
  codigo             String   @unique
>>>>>>> de36ec36e6a7e9fbef2f70ab812edf8c8d616756
  nombre             String?
  hectareas          Float?
  ubicacion          String?
  observacion        String?
<<<<<<< HEAD
  activo             Boolean       @default(true)
  createdAt          DateTime      @default(now())
  updatedAt          DateTime      @updatedAt
  horas_fermentacion Float?
  horas_oxidacion    Float?
  tipo_cafe          String        @default("comercial")
  cosechaLotes       CosechaLote[]
=======
  activo             Boolean  @default(true)
  tipo_cafe          String   @default("comercial")
  horas_oxidacion    Float?
  horas_fermentacion Float?

  cosechaLotes CosechaLote[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
>>>>>>> de36ec36e6a7e9fbef2f70ab812edf8c8d616756
}

model CosechaLote {
  id        Int      @id @default(autoincrement())
  cosechaId Int
  loteId    Int
  createdAt DateTime @default(now())
  cosecha   Cosecha  @relation(fields: [cosechaId], references: [id], onDelete: Cascade)
  lote      Lote     @relation(fields: [loteId], references: [id])

  @@unique([cosechaId, loteId])
}

model Usuario {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  password  String
  nombre    String
  rol       String   @default("ADMIN")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProcesoTrazabilidad {
  id               Int      @id @default(autoincrement())
  fecha            DateTime
  etapa            String
  kilosIngresados  Float
  kilosResultantes Float
  porcentajeMerma  Float
  cosechaId        Int
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
  cosecha          Cosecha  @relation(fields: [cosechaId], references: [id], onDelete: Cascade)
}

model Cliente {
  id               Int      @id @default(autoincrement())
  dni_rut          String   @unique
  nombre           String
  persona_juridica Boolean  @default(false)
  createdAt        DateTime @default(now())
  updatedAt        DateTime
}

model Trabajador {
  id        Int       @id @default(autoincrement())
  nombres   String
  dni       String    @unique
  rol       String?
  createdAt DateTime  @default(now())
  updatedAt DateTime
  Cosecha   Cosecha[]
}

model Usuario {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  password  String
  nombre    String
  rol       String   @default("ADMIN")
  createdAt DateTime @default(now())
<<<<<<< HEAD
  updatedAt DateTime
}
=======
  updatedAt DateTime @updatedAt
}

model Trabajador {
  id        Int       @id @default(autoincrement())
  nombres   String
  dni       String    @unique
  rol       String?
  cosechas  Cosecha[]
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}

model Cliente {
  id               Int      @id @default(autoincrement())
  dni_rut          String   @unique
  nombre           String
  persona_juridica Boolean  @default(false)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
}
>>>>>>> de36ec36e6a7e9fbef2f70ab812edf8c8d616756
